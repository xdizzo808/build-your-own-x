app = "your-app-name" # Change this to your Fly.io app name
primary_region = "ord" # You can change "ord" to a closer city (like "lax" or "lhr")

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[env]
  PORT = "3000"
  NODE_ENV = "production"
  
